# - Config file for the superray package
#
#  Usage from an external project:
#    In your CMakeLists.txt, add these lines:
#
#    FIND_PACKAGE(superray REQUIRED )
#    INCLUDE_DIRECTORIES(${SUPERRAY_INCLUDE_DIRS})
#    TARGET_LINK_LIBRARIES(MY_TARGET_NAME ${SUPERRAY_LIBRARIES})
#
# It defines the following variables
#  SUPERRAY_INCLUDE_DIRS - include directories for superray
#  SUPERRAY_LIBRARY_DIRS - library directories for superray (normally not used!)
#  SUPERRAY_LIBRARIES    - libraries to link against

@PACKAGE_INIT@

# Tell the user project where to find our headers and libraries
set_and_check(SUPERRAY_INCLUDE_DIRS "@PACKAGE_SUPERRAY_INCLUDE_DIRS@")
set_and_check(SUPERRAY_LIBRARY_DIRS "@PACKAGE_SUPERRAY_LIB_DIR@")

set(SUPERRAY_LIBRARIES "@PACKAGE_SUPERRAY_LIB_DIR@/@SUPERRAY_LIBRARY@")
